# Copy this file to .env and adjust as needed.
# All values shown are the defaults — only set what you want to override.

# Host ports (container-side ports are fixed)
FRONTEND_PORT=80
BACKEND_PORT=8000
MCP_PORT=8001

# Allowed CORS origins for the backend (JSON array)
BB_CORS_ORIGINS=["http://localhost"]
