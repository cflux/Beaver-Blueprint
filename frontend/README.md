# Beaver Blueprint — Frontend

React + Vite + TypeScript + Tailwind CSS

## Setup

```bash
cd frontend
npm install
```

## Run dev server

```bash
npm run dev
```

Runs at `http://localhost:5173`. API requests are proxied to `http://localhost:8000`.

## Build for production

```bash
npm run build
```

Output goes to `dist/`.
